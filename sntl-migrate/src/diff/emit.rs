//! Emit module — populated in Task 11.

use super::compare::Change;

/// Placeholder — full implementation lands in Task 11.
pub fn emit(_changes: &[Change]) -> (String, usize) {
    (String::new(), 0)
}
