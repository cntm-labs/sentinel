name: Publish Crates (Manual)

on:
  workflow_dispatch:
    inputs:
      dry-run:
        description: "Dry run (no actual publish)"
        required: false
        default: "false"
        type: choice
        options:
          - "false"
          - "true"

jobs:
  publish:
    runs-on: ubuntu-latest
    environment: crates-io
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2

      - name: Verify tests pass
        run: cargo test --workspace

      - name: Publish sntl-core
        run: cargo publish -p sntl-core ${{ inputs.dry-run == 'true' && '--dry-run' || '' }}
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}

      - name: Wait for crates.io index (sntl-core)
        if: inputs.dry-run == 'false'
        run: sleep 30

      - name: Publish sntl-macros
        run: cargo publish -p sntl-macros --no-verify ${{ inputs.dry-run == 'true' && '--dry-run' || '' }}
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}

      - name: Wait for crates.io index (sntl-macros)
        if: inputs.dry-run == 'false'
        run: sleep 30

      - name: Publish sntl-migrate
        run: cargo publish -p sntl-migrate ${{ inputs.dry-run == 'true' && '--dry-run' || '' }}
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}

      - name: Wait for crates.io index (sntl-migrate)
        if: inputs.dry-run == 'false'
        run: sleep 30

      - name: Publish sntl
        run: cargo publish -p sntl ${{ inputs.dry-run == 'true' && '--dry-run' || '' }}
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}

      - name: Wait for crates.io index (sntl)
        if: inputs.dry-run == 'false'
        run: sleep 30

      - name: Publish sntl-cli
        run: cargo publish -p sntl-cli ${{ inputs.dry-run == 'true' && '--dry-run' || '' }}
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}
